<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/management_approval_recycler"
        style="@style/height_wrap_parent"
        app:layout_constraintBottom_toTopOf="@+id/btn_submit_approval"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/btn_submit_approval"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0"
        android:layout_marginBottom="@dimen/dp50"/>


    <include layout="@layout/internet_not_found"/>

    <Button
        android:id="@+id/btn_submit_approval"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dp50"
        android:visibility="gone"
        android:background="@color/colorapptop"
        android:text="Submit"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>